<Window
    x:Class="FODevManager.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FODevManager.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FO Dev Manager">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Border Background="{ThemeResource SystemBaseLowColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <!-- Title Bar -->
                <RowDefinition Height="*"/>
                <!-- Content -->
                <RowDefinition Height="Auto"/>
                <!-- Status Bar -->
            </Grid.RowDefinitions>

            <!-- 🔷 Custom Title Bar -->
            <Grid x:Name="AppTitleBar" Grid.Row="0"
                  Background="Transparent"
                  BorderBrush="{ThemeResource SystemBaseLowColor}"
                  BorderThickness="0,0,0,1"
                  Padding="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                    <FontIcon Glyph="&#xE8A5;" FontSize="18" />
                    <TextBlock Text="FO Dev Manager" FontSize="16" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>

            <!-- 🔶 Main Content -->
            <Grid Grid.Row="1" Padding="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Row 0: Profile Selection & Open Solution -->
                    <RowDefinition Height="Auto"/>
                    <!-- Row 1: DB name & Is Active -->
                    <RowDefinition Height="*"/>
                    <!-- Row 2: Models List fills remaining height -->
                    <RowDefinition Height="Auto"/>
                    <!-- Row 3: Add Model -->
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 🔷 Row 0: Profile Selection + Open Solution -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="10">
                    <ComboBox x:Name="ProfilesDropdown" Width="300" SelectionChanged="ProfilesDropdown_SelectionChanged"/>
                    <Button Content="Create Profile" Click="CreateProfile_Click" />
                    <Button Content="Deploy Profile" Click="DeployProfile_Click" />
                    <Button Content="UnDeploy Profile" Click="UnDeployProfile_Click" />
                </StackPanel>

                <Button Grid.Row="0" Grid.Column="1" Content="Open Solution" Click="OpenSolution_Click" Margin="10,0,0,0" />

                <!-- 🔷 Row 1: Database Name & Is Active -->
                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="10" Margin="0,10,0,10">
                    <TextBlock Text="Database:" VerticalAlignment="Center" />
                    <TextBox x:Name="DatabaseNameTextBox" Width="200" IsReadOnly="True" />
                    <CheckBox x:Name="IsActiveCheckBox" Content="Is Active" IsEnabled="False" VerticalAlignment="Center" />
                </StackPanel>

                <!-- 🔷 Row 2: Models List -->
                <Border Grid.Row="2" Grid.ColumnSpan="2" BorderThickness="1" BorderBrush="{ThemeResource SystemBaseLowColor}">
                    <ListView x:Name="ModelsListView" SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock Text="{Binding ModelName}" Width="200"/>
                                    <TextBlock Text="{Binding IsDeployed, Converter={StaticResource BoolToStatusConverter}}" Width="100"/>
                                    <Button Content="Remove" Click="RemoveModel_Click" Tag="{Binding ModelName}" Margin="10,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <!-- 🔷 Row 3: Add Model -->
                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                    <TextBox x:Name="ModelPathTextBox" Width="300" PlaceholderText="Browse model folder..." IsReadOnly="True" />
                    <Button Content="Browse" Click="BrowseModel_Click" />
                    <Button Content="Add Model" Click="AddModel_Click" />
                </StackPanel>
            </Grid>


            <!-- 🔻 Status Bar -->
            <Border Grid.Row="2" Background="{ThemeResource SystemBaseLowColor}" Padding="6,4" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="StatusBar" Text="Ready" FontWeight="Bold" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Content="View Logs" Click="ViewLogs_Click" />
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>
