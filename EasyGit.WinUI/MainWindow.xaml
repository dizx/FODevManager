<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EasyGit.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyGit.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="EasyGit">

    <Grid RowSpacing="12">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#F4F6FA" Offset="0" />
                <GradientStop Color="#E8ECF3" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="10">
                <TextBlock Text="EasyGit" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Automated Git workflow for FO developer profiles" Opacity="0.75" />

                <Border CornerRadius="12" Background="#C6CDD8" Padding="10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ComboBox x:Name="ProfilesDropdown" Width="320" SelectionChanged="ProfilesDropdown_SelectionChanged" PlaceholderText="Select profile" />
                        <Button Content="Refresh" Style="{StaticResource SecondaryActionButtonStyle}" Click="Refresh_Click" />
                        <TextBlock Text="Auto sync is active" VerticalAlignment="Center" Opacity="0.8" FontStyle="Italic" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <ListView Grid.Row="1" x:Name="RepositoriesList" SelectionMode="None" Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:RepoRowViewModel">
                        <Border Margin="0,4,0,4" Padding="12" CornerRadius="12" Background="#D7DCE3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="260" />
                                    <ColumnDefinition Width="230" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Spacing="3">
                                    <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold" FontSize="18" />
                                    <TextBlock Text="{x:Bind BranchInfo}" FontStyle="Italic" Opacity="0.75" />
                                </StackPanel>

                                <Border Grid.Column="1" Background="#EDF2FA" CornerRadius="10" Padding="8,4" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind StatusText}" />
                                </Border>

                                <Button Grid.Column="2" Margin="10,0,0,0" Content="Create Task/Feature" Tag="{x:Bind RepoId}" Style="{StaticResource PrimaryActionButtonStyle}" Click="CreateFeature_Click" />
                                <Button Grid.Column="3" Margin="8,0,0,0" Content="Commit" Tag="{x:Bind RepoId}" Style="{StaticResource PrimaryActionButtonStyle}" Click="Commit_Click" IsEnabled="{x:Bind CanCommit}" />
                                <Button Grid.Column="4" Margin="8,0,0,0" Content="Create PR" Tag="{x:Bind RepoId}" Style="{StaticResource PrimaryActionButtonStyle}" Click="CreatePr_Click" IsEnabled="{x:Bind CanCreatePr}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border Grid.Row="2" Background="#D2D8E2" CornerRadius="8" Padding="10,6">
                <TextBlock x:Name="StatusTextBlock" Text="Ready." Opacity="0.85" />
            </Border>
        </Grid>
    </Grid>
</Window>
